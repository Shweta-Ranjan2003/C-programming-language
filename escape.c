#include <stdio.h>
int main()
{
    /*escape sequences are 2 or more character starting with / it doesn't represents itself.
    for example /n=for next line
    \a=for alarm or beep
    \t=for tab (gives tab space)
    \b=for backspace
    \\=for backslash
    \'=single quote
    \"=double quote
    */
   printf("see tab \t\t\t\t , see backslash \\ \\n , listen alarm \a , ");
    return 0;
}
