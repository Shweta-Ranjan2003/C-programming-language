#include <stdio.h>
int main(int argc, char const *argv[])
{
    //token is every single elements in a program. Basic building blocks.
    //token are classified into keywords,strings,identifier,constants,special characters,operators.
    //identifiers are user defined and are used to give name to constants,variable,functions.
    //identifier consists of letter and number and starts with letter or underscore , no blanks.
    //keywords (there are 32 reserved keywords) are in lowercase.
    /* auto , double , int , struct
      break , else , long , switch
      case , enum  , register , typedef
      char , extern , return , union
      const , float , short , unsigned
      continue , for , signed , void
      default , goto , sizeof , volatile
      do , if , static , while 
   */
    return 0;
}

